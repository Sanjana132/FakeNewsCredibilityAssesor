# ── Fake News & Source Credibility Detector — Test / CI Requirements ───────
# Minimal dependency set needed to run the pytest suite in tests/.
#
# The unit tests exercise only the data pipeline and feature engineering
# (data_pipeline.py, config.py, utils/seed.py). They do NOT
# import torch, transformers, faiss, gradio, shap, fastapi, etc. — so CI
# installs just this lean set for a fast, deterministic run instead of the full
# requirements.api.txt (whose heavy binary deps are slow and fragile to resolve).

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
scipy>=1.10.0
nltk>=3.8.0
vaderSentiment>=3.3.2
langdetect>=1.0.9
matplotlib>=3.7.0
seaborn>=0.13.0
pytest>=7.4.0
