# ── Fake News & Source Credibility Detector — API/Inference Requirements ──
# Install: pip install -r requirements.api.txt
# Used by: Dockerfile.api, local inference, CI tests

# ── Core ML (inference only, no training deps) ─────────────────────────────
torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99        # DeBERTa-v3 slow tokenizer (silently required)
protobuf>=3.20.0             # required by sentencepiece

# ── Data ───────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0          # TF-IDF baseline + scaler
scipy>=1.10.0                # calibration, sparse matrices
joblib>=1.2.0                # load pickled baseline model

# ── NLP / features ─────────────────────────────────────────────────────────
nltk>=3.8.0
vaderSentiment>=3.3.2
langdetect>=1.0.9

# ── API server ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
slowapi>=0.1.9               # rate limiting
python-dotenv>=1.0.0

# ── Caching & observability ─────────────────────────────────────────────────
redis>=5.0.0
prometheus-client>=0.19.0

# ── Agent / retrieval tools ─────────────────────────────────────────────────
aiohttp>=3.9.0
sentence-transformers>=2.3.0  # FAISS embedding model
faiss-cpu>=1.7.4              # FAISS index (cpu); use faiss-gpu on CUDA
langgraph>=0.1.0
langchain-core>=0.1.0

# ── Explainability ─────────────────────────────────────────────────────────
shap>=0.43.0

# ── Demo UI ────────────────────────────────────────────────────────────────
gradio>=4.20.0

# ── Misc ───────────────────────────────────────────────────────────────────
tqdm>=4.65.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
